# 1. 硬件目标定义 (24.10 / 6.6 内核标准写法)
# =========================================================
CONFIG_TARGET_mediatek=y
CONFIG_TARGET_mediatek_filogic=y
CONFIG_TARGET_mediatek_filogic_DEVICE_cmcc_rax3000m=y

# QModem 主程序与 LuCI 界面
# 1. QModem-Next 核心 (新一代管理界面)
# =========================================================
# 勾选 Next 版，会自动取消掉旧版
CONFIG_PACKAGE_luci-app-qmodem-next=y
# 依然需要核心后端支持
CONFIG_PACKAGE_qmodem=y

# QModem 短信管理组件 (支持查看、发送、转发短信)
CONFIG_PACKAGE_luci-app-qmodem-sms=y
CONFIG_PACKAGE_sms-tool=y

# QModem 链路健康检查与多网切换 (针对 5G/有线备份)
CONFIG_PACKAGE_luci-app-qmodem-hc=y

# QModem 统计图表 (流量监控、频段分析)
CONFIG_PACKAGE_luci-app-qmodem-stats=y

# NCM 协议核心驱动 (Kernel 6.6 优化版)
CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=y

# USB 串口控制与模式转换 (用于发送 AT 指令)
CONFIG_PACKAGE_kmod-usb-serial-option=y
CONFIG_PACKAGE_kmod-usb-serial-wwan=y
CONFIG_PACKAGE_usb-modeswitch=y


CONFIG_PACKAGE_usbutils=y


# 拨号协议支持库
CONFIG_PACKAGE_comgt=y
CONFIG_PACKAGE_comgt-ncm=y

# 你的“三剑客”插件
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_luci-app-adguardhome=y
CONFIG_PACKAGE_luci-app-smartdns=y

# TCP BBR 拥塞控制 (解决 5G 延迟抖动，提升极限吞吐)
CONFIG_KERNEL_TCP_CONG_BBR=y
CONFIG_KERNEL_TCP_CONG_ADVANCED=y

# MT7981 专用的 XHCI 控制器驱动 (必选)
CONFIG_PACKAGE_kmod-usb-xhci-mtk=y
# USB 3.0 支持
CONFIG_PACKAGE_kmod-usb3=y
# 虽然 RM500U 是 3.0，但为了兼容性，2.0 驱动也建议带上
CONFIG_PACKAGE_kmod-usb2=y


# 某些固件环境下，cdc_ether 是 cdc_ncm 的基石
CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y
# 网络协议核心
CONFIG_PACKAGE_wwan=y

# IPv6 NAT 转换 (必选，解决 5G IPv6 共享问题)
CONFIG_PACKAGE_kmod-ipt-nat6=y
CONFIG_PACKAGE_ip6tables-mod-nat=y
# IPv6 路由与邻居发现
CONFIG_PACKAGE_kmod-ip6tables=y

# 流量重定向与透明代理核心
CONFIG_PACKAGE_kmod-ipt-tproxy=y
CONFIG_PACKAGE_kmod-ipt-extra=y
CONFIG_PACKAGE_iptables-mod-tproxy=y
CONFIG_PACKAGE_iptables-mod-extra=y
# OpenClash 的核心核心 (必须手动指定)
CONFIG_PACKAGE_coreutils-nohup=y
CONFIG_PACKAGE_bash=y
CONFIG_PACKAGE_curl=y

# 查看 USB 设备是否掉线、是否识别为 USB 3.0
CONFIG_PACKAGE_usbutils=y  # 提供 lsusb 命令
# 查看流量实时分配
CONFIG_PACKAGE_iftop=y
# 手动发送 AT 指令调试 (即使插件红了，你也能手动救火)
CONFIG_PACKAGE_picocom=y

# MTK 硬件流量卸载 (必须勾选)
CONFIG_PACKAGE_kmod-mtk-offload=y
# 软件流量分流 (建议也带上，作为冗余)
CONFIG_PACKAGE_kmod-shortcut-fe=y
CONFIG_PACKAGE_kmod-shortcut-fe-cm=y

# 核心数据处理工具 (必选，否则 QModem 界面大概率报错)
CONFIG_PACKAGE_jq=y

# 允许模组在空闲时不进入深度睡眠 (防止掉线)
CONFIG_PACKAGE_kmod-usb-net-cdc-wdm=y
# USB 核心调试工具 (lsusb 的基础)
CONFIG_PACKAGE_kmod-usb-core=y

# 开启虚拟内存压缩，大幅提升多插件运行稳定性
CONFIG_PACKAGE_kmod-zram=y
CONFIG_PACKAGE_zram-swap=y

# 1. 彻底移除干扰项 (预防串口抢占与拨号死锁)
# =========================================================
# 必须禁用：防止其抢占 /dev/ttyUSB 串口导致拨号失败 
# CONFIG_PACKAGE_modemmanager is not set
# CONFIG_PACKAGE_luci-proto-modemmanager is not set

# 禁用 USB Modem Server：防止与原生 NCM 协议产生逻辑冲突 
# CONFIG_PACKAGE_luci-app-usb-modem is not set

# 2. 极致瘦身：移除 5G 环境下的无用功 (腾出空间给三剑客) 
# =========================================================
# 移除 UPnP (5G 大局域网环境下完全无效) 
# CONFIG_PACKAGE_luci-app-upnp is not set
# CONFIG_PACKAGE_miniupnpd is not set

# 移除 Samba/网络共享 (节省约 5MB 空间) 
# CONFIG_PACKAGE_luci-app-ksmbd is not set
# CONFIG_PACKAGE_ksmbd-server is not set

# 移除 打印服务器
# CONFIG_PACKAGE_luci-app-usb-printer is not set
# CONFIG_PACKAGE_luci-app-p910nd is not set

# 3. 界面精简 (防止静态资源堆积)
# =========================================================
# 建议只保留默认主题，移除所有第三方重型主题 
# CONFIG_PACKAGE_luci-theme-argon is not set
# CONFIG_PACKAGE_luci-app-argon-config is not set

# 4. 高级维护与分区解锁 (老兵进阶专用)
# =========================================================
# 解锁只读分区：允许修改 Factory、U-Boot 等，方便底层维护 
CONFIG_PACKAGE_kmod-mtd-rw=y

# 5. 诊断工具补齐
# =========================================================
CONFIG_PACKAGE_pciutils=y
CONFIG_PACKAGE_lsusb=y
CONFIG_PACKAGE_nano=y


